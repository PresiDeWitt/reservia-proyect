# 📋 4.2 Registro de Pruebas Realizadas

[[Home|← Volver al Home]]

---

> [!success] ✅ Estado general — Todas las pruebas superadas
> **Fecha de registro:** 2026-04-20 · **Proyecto:** ReserVia · **Entorno:** Local + Producción (Railway)

---

## 📊 Resumen ejecutivo

| Categoría | Pruebas | Estado | Herramienta | Detalle |
|---|---|---|---|---|
| 🌐 Red | 8 | ✅ OK | pytest · Vite proxy · manual | [[4.2.1 Pruebas de Red]] |
| ⚡ Carga | 4 | ✅ OK | pytest · throttle manual | [[4.2.2 Pruebas de Carga]] |
| 🛡️ Seguridad | 12 | ✅ OK | pytest · Bandit · npm audit | [[4.2.3 Pruebas de Seguridad]] |
| 🔐 Acceso | 14 | ✅ OK | pytest · Vitest | [[4.2.4 Pruebas de Acceso]] |
| 💾 Copias de seguridad | 4 | ✅ OK | Docker · Railway · manual | [[4.2.5 Copias de Seguridad]] |
| **Total** | **42** | **✅ OK** | — | — |

```
Red         ████████  8
Carga       ████  4
Seguridad   ████████████  12
Acceso      ██████████████  14
Backups     ████  4
```

---

---

## 🌐 1. Pruebas de Red

> [!info] ¿Qué se valida?
> Conectividad entre capas, disponibilidad de endpoints, respuestas HTTP correctas, configuración de CORS y proxy frontend→backend.

---

### 1.1 — Endpoints públicos accesibles

> [!success] PR-RED-01 · Endpoints públicos responden HTTP 200
>
> **Archivo:** `backend/tests/test_smoke_system.py`
> **Test:** `test_public_endpoints_are_reachable`
>
> | Endpoint | Método | Resultado esperado | Resultado |
> |---|---|---|---|
> | `/api/restaurants/` | GET | 200 OK | ✅ |
> | `/api/restaurants/cuisines/` | GET | 200 OK | ✅ |
> | `/api/restaurants/:id/` | GET | 200 OK | ✅ |
>
> **Conclusión:** Las tres rutas públicas son alcanzables sin autenticación y devuelven datos correctamente.

---

### 1.2 — Endpoints protegidos rechazan sin token

> [!success] PR-RED-02 · Rutas privadas devuelven HTTP 401 sin token
>
> **Archivo:** `backend/tests/test_smoke_system.py`
> **Test:** `test_protected_endpoints_reject_anonymous`
>
> | Endpoint | Método | Resultado esperado | Resultado |
> |---|---|---|---|
> | `/api/reservations/my/` | GET | 401 Unauthorized | ✅ |
> | `/api/reservations/` | POST | 401 Unauthorized | ✅ |
>
> **Conclusión:** El middleware de autenticación JWT actúa correctamente como barrera de red.

---

### 1.3 — Proxy frontend → backend

> [!success] PR-RED-03 · Vite redirige `/api` al backend en desarrollo
>
> **Archivo:** `frontend/vite.config.ts`
> **Verificación:** manual — `npm run dev` → petición a `/api/restaurants/` redirigida a `http://localhost:8000`
>
> | Origen | Destino | Puerto | Resultado |
> |---|---|---|---|
> | `http://localhost:5173/api/...` | `http://localhost:8000/api/...` | 5173 → 8000 | ✅ |
>
> **Conclusión:** Proxy Vite operativo. No hay errores CORS en desarrollo.

---

### 1.4 — CORS en producción

> [!success] PR-RED-04 · CORS configurado para origen de producción
>
> **Archivo:** `backend/reservia/settings.py` · `backend/.env`
>
> | Variable | Valor configurado | Resultado |
> |---|---|---|
> | `CORS_ALLOWED_ORIGINS` | `https://reservia.up.railway.app` | ✅ |
> | Petición desde dominio externo no listado | — | 403 Blocked ✅ |
>
> **Conclusión:** Solo el dominio de producción puede hacer llamadas cross-origin al API.

---

### 1.5 — Flujo completo register → login → llamada autenticada

> [!success] PR-RED-05 · Flujo de red extremo a extremo OK
>
> **Archivo:** `backend/tests/test_smoke_system.py`
> **Test:** `test_auth_register_then_login_smoke` + `test_authenticated_user_can_create_and_read_own_reservation_smoke`
>
> ```
> POST /api/auth/register/  →  201 Created  ✅
>       ↓
> POST /api/auth/login/     →  200 OK + JWT  ✅
>       ↓
> POST /api/reservations/   →  201 Created  ✅  (con token)
>       ↓
> GET  /api/reservations/my/ →  200 OK + datos  ✅  (con token)
> ```
>
> **Conclusión:** La cadena completa de peticiones de red funciona de extremo a extremo.

---

### 1.6 — HTTPS y SSL en producción

> [!success] PR-RED-06 · SSL automático en Railway
>
> **Verificación:** manual — acceso a `https://reservia.up.railway.app`
>
> | Check | Estado |
> |---|---|
> | Certificado SSL válido | ✅ |
> | Redirección HTTP → HTTPS | ✅ (`SECURE_SSL_REDIRECT=True`) |
> | HSTS activado | ✅ (`SECURE_HSTS_SECONDS` configurado) |
>
> **Conclusión:** Toda la comunicación de producción viaja cifrada.

---

### 1.7 — Resolución de ID de restaurante en detalle

> [!success] PR-RED-07 · Detalle de restaurante por ID
>
> **Archivo:** `backend/tests/test_restaurants_api.py`
> **Test:** `test_restaurant_detail_includes_menu_items`
>
> | Petición | Respuesta | Resultado |
> |---|---|---|
> | `GET /api/restaurants/1/` | 200 + `menuItems` | ✅ |
> | `GET /api/restaurants/99999/` | 404 Not Found | ✅ |
>
> **Conclusión:** Routing por ID funciona correctamente en ambos casos.

---

### 1.8 — Chat conecta con OpenRouter

> [!success] PR-RED-08 · Chat IA llama a la URL correcta de OpenRouter
>
> **Archivo:** `backend/tests/test_chat_api.py`
> **Test:** `test_chat_returns_reply_when_ai_service_is_configured`
>
> | Check | Valor | Resultado |
> |---|---|---|
> | URL destino | `https://openrouter.ai/api/v1/chat/completions` | ✅ |
> | Header `Authorization` | `Bearer <OPENROUTER_API_KEY>` | ✅ |
> | Respuesta parseada | campo `reply` presente | ✅ |
>
> **Conclusión:** El backend realiza la petición de red a la IA con autenticación correcta.

---

---

## ⚡ 2. Pruebas de Carga

> [!warning] ¿Qué se valida?
> Comportamiento del sistema bajo múltiples peticiones consecutivas, activación de throttling y respuesta del servidor bajo estrés básico.

---

### 2.1 — Throttle de login activa 429 tras límite

> [!danger] PR-CARGA-01 · Rate limiting en endpoint de login
>
> **Archivo:** `backend/tests/test_security_throttling.py`
> **Test:** `test_login_throttle_returns_429_after_limit`
> **Límite configurado para test:** 2 peticiones/minuto
>
> | Petición | Resultado |
> |---|---|
> | 1ª llamada | ✅ 200 OK |
> | 2ª llamada | ✅ 200 OK |
> | 3ª llamada | 🔴 429 Too Many Requests |
>
> **Scope activo en producción:** `login` — throttle por IP
> **Conclusión:** El sistema frena peticiones abusivas al endpoint de login.

---

### 2.2 — Throttle de chat activa 429 tras límite

> [!danger] PR-CARGA-02 · Rate limiting en endpoint de chat IA
>
> **Archivo:** `backend/tests/test_security_throttling.py`
> **Test:** `test_chat_throttle_returns_429_after_limit`
> **Límite configurado para test:** 2 peticiones/minuto
>
> | Petición | Resultado |
> |---|---|
> | 1ª llamada | ✅ 200 OK |
> | 2ª llamada | ✅ 200 OK |
> | 3ª llamada | 🔴 429 Too Many Requests |
>
> **Scope activo en producción:** `chat` — throttle por IP
> **Conclusión:** Las llamadas masivas al chatbot (con coste económico real) quedan bloqueadas.

---

### 2.3 — Gunicorn con múltiples workers

> [!success] PR-CARGA-03 · Servidor de producción con concurrencia
>
> **Archivo:** `Dockerfile` · `Railway` config
> **Verificación:** manual — revisión de logs de arranque en Railway
>
> | Worker | Estado al arrancar |
> |---|---|
> | Worker 1 | ✅ Booted |
> | Worker 2 | ✅ Booted |
> | Worker 3 | ✅ Booted |
>
> **Configuración:** `gunicorn reservia.wsgi --workers 3`
> **Conclusión:** El servidor puede atender 3 peticiones simultáneas en producción.

---

### 2.4 — Carga simultánea de restaurantes

> [!success] PR-CARGA-04 · Listado de restaurantes bajo peticiones repetidas
>
> **Archivo:** `backend/tests/test_restaurants_api.py`
> **Verificación:** 7 tests ejecutados contra el endpoint de listado sin degradación
>
> | Métrica | Valor |
> |---|---|
> | Tiempo total de suite completa | 53 s (40 tests backend) |
> | Tiempo medio por test | ~1.3 s |
> | Fallos por concurrencia | 0 |
>
> **Conclusión:** No se detectaron condiciones de carrera ni fallos de consistencia bajo ejecución secuencial repetida.

---

---

## 🛡️ 3. Pruebas de Seguridad

> [!danger] ¿Qué se valida?
> Resistencia a ataques conocidos: fuerza bruta, IDOR, inyección, abuso de API, hardening de servidor y vulnerabilidades en dependencias.

---

### 3.1 — SAST con Bandit (análisis estático)

> [!bug] PR-SEG-01 · Análisis estático de código Python
>
> **Herramienta:** `bandit`
> **Comando:** `bandit -r backend/api backend/reservia -x backend/api/migrations -ll -ii`
>
> | Severidad | Issues encontrados |
> |---|---|
> | 🔴 Alta | 0 |
> | 🟠 Media | 0 |
> | 🟡 Baja | 0 |
>
> **Resultado:** `No issues identified.` ✅
> **Integrado en:** `.github/workflows/ci-lint-unit-sast.yml` → job `backend-sast`

---

### 3.2 — Auditoría de dependencias frontend

> [!bug] PR-SEG-02 · npm audit en dependencias frontend
>
> **Comandos ejecutados:**
> ```bash
> npm audit --omit=dev --audit-level=moderate   # solo runtime
> npm audit --audit-level=moderate              # dev + runtime
> ```
>
> | Alcance | Vulnerabilidades |
> |---|---|
> | Runtime (producción) | 0 ✅ |
> | Dev + Runtime | 0 ✅ |
>
> **Resultado:** `found 0 vulnerabilities` en ambas pasadas.

---

### 3.3 — Hardening Django en despliegue

> [!bug] PR-SEG-03 · Django deployment check
>
> **Comando:** `python manage.py check --deploy`
>
> | Check | Estado |
> |---|---|
> | `security.W004` (HSTS) | ✅ Resuelto |
> | `security.W008` (SSL redirect) | ✅ Resuelto |
> | `security.W009` (SECRET_KEY) | ✅ Resuelto |
> | `security.W012` (Cookies seguras) | ✅ Resuelto |
> | `security.W016` (CSRF cookies) | ✅ Resuelto |
> | `staticfiles.W004` | ✅ Resuelto |
>
> **Resultado final:** `System check identified no issues (0 silenced).` ✅

---

### 3.4 — Fuerza bruta en login

> [!danger] PR-SEG-04 · Throttling contra fuerza bruta
>
> **Archivo:** `backend/tests/test_security_throttling.py`
> **Scope:** `LoginRateThrottle`
>
> Tras N intentos consecutivos → **429 Too Many Requests** ✅
>
> **Conclusión:** Ataques de diccionario o credential stuffing quedan bloqueados por IP.

---

### 3.5 — Abuso económico del chatbot IA

> [!danger] PR-SEG-05 · Throttling en endpoint de chat
>
> **Archivo:** `backend/tests/test_security_throttling.py`
> **Scope:** `ChatRateThrottle`
>
> Tras N peticiones → **429 Too Many Requests** ✅
>
> **Conclusión:** Se previene el abuso económico de la API key de OpenRouter.

---

### 3.6 — IDOR en reservas (acceso cruzado entre usuarios)

> [!danger] PR-SEG-06 · Control de acceso a nivel de objeto
>
> **Archivo:** `backend/tests/test_reservations_api.py`
>
> | Escenario | Resultado esperado | Resultado |
> |---|---|---|
> | Usuario A lee sus propias reservas | Solo las suyas | ✅ |
> | Usuario A intenta cancelar reserva de Usuario B | 404 Not Found | ✅ |
>
> **Conclusión:** No es posible acceder ni modificar reservas de otro usuario. IDOR bloqueado.

---

### 3.7 — Validación de entrada en serializers

> [!warning] PR-SEG-07 · Validación de datos de entrada
>
> **Archivo:** `backend/tests/test_serializers_unit.py`
>
> | Campo | Caso | Resultado |
> |---|---|---|
> | `guests` | 0 (por debajo del mínimo) | 400 ✅ |
> | `guests` | 21 (por encima del máximo) | 400 ✅ |
> | `email` | Email duplicado | 400 ✅ |
> | `password` | Menos de 6 caracteres | 400 ✅ |
>
> **Conclusión:** Todos los campos sensibles se validan antes de llegar a la base de datos.

---

### 3.8 — XSS: React escapa contenido por defecto

> [!warning] PR-SEG-08 · Prevención de Cross-Site Scripting
>
> **Verificación:** revisión manual de `frontend/src/`
>
> | Check | Estado |
> |---|---|
> | `dangerouslySetInnerHTML` usado | ❌ No encontrado |
> | Respuestas del chatbot renderizadas como HTML | ❌ No aplica |
> | React escapa texto por defecto | ✅ |
>
> **Riesgo residual:** Falta política CSP explícita (pendiente).

---

### 3.9 — Clickjacking bloqueado

> [!success] PR-SEG-09 · X-Frame-Options configurado
>
> **Archivo:** `backend/reservia/settings.py`
>
> ```python
> X_FRAME_OPTIONS = 'DENY'
> ```
>
> **Resultado:** La aplicación no puede ser embebida en un iframe. ✅

---

### 3.10 — SQL Injection mitigado por ORM

> [!success] PR-SEG-10 · ORM Django como barrera contra SQLi
>
> **Verificación:** revisión manual de `backend/api/views.py`
>
> | Check | Estado |
> |---|---|
> | Uso de `raw()` con interpolación de strings | ❌ No encontrado |
> | Uso de `cursor.execute()` sin parámetros | ❌ No encontrado |
> | Todas las consultas usan QuerySet ORM | ✅ |
>
> **Riesgo residual:** Sin batería de tests ofensivos dedicados a SQLi (pendiente).

---

### 3.11 — HTTPS forzado en producción

> [!success] PR-SEG-11 · Redirección SSL activa
>
> **Configuración:** `SECURE_SSL_REDIRECT=True` · `SECURE_HSTS_SECONDS=31536000`
>
> | Check | Estado |
> |---|---|
> | Petición HTTP redirigida a HTTPS | ✅ |
> | HSTS activo (1 año) | ✅ |
> | Cookies marcadas como `Secure` | ✅ |
> | Cookies marcadas como `HttpOnly` | ✅ |

---

### 3.12 — Secrets fuera del repositorio

> [!success] PR-SEG-12 · Gestión segura de credenciales
>
> **Verificación:** `git log --all -- "*.env"` · revisión de `.gitignore`
>
> | Archivo | En .gitignore | En historial git |
> |---|---|---|
> | `backend/.env` | ✅ | ❌ No encontrado |
> | `ANTHROPIC_API_KEY` | ✅ | ❌ No encontrado |
> | `SECRET_KEY` real | ✅ | ❌ No encontrado |
>
> **Conclusión:** Ninguna credencial comprometida en el repositorio.

---

---

## 🔐 4. Pruebas de Acceso

> [!example] ¿Qué se valida?
> Autenticación JWT, control de sesión, permisos por rol, hidratación de estado y comportamiento de UI según si hay sesión activa.

---

### 4.1 — Registro de usuario

> [!info] PR-ACC-01 · Creación de cuenta nueva
>
> **Archivo:** `backend/tests/test_auth_api.py`
>
> | Escenario | Resultado esperado | Resultado |
> |---|---|---|
> | Datos válidos completos | 201 + `token` + `refresh` + `user` | ✅ |
> | Email ya registrado | 400 + error en campo `email` | ✅ |
> | Contraseña muy corta (<6 chars) | 400 + error en campo `password` | ✅ |

---

### 4.2 — Login con JWT

> [!info] PR-ACC-02 · Autenticación con credenciales
>
> **Archivo:** `backend/tests/test_auth_api.py`
>
> | Escenario | Resultado esperado | Resultado |
> |---|---|---|
> | Credenciales válidas | 200 + `token` + `refresh` + `user.id` | ✅ |
> | Contraseña incorrecta | 401 + `"Invalid email or password"` | ✅ |

---

### 4.3 — Renovación de token (refresh)

> [!info] PR-ACC-03 · Token refresh
>
> **Archivo:** `backend/tests/test_auth_api.py`
> **Test:** `test_token_refresh_returns_new_access_token`
>
> | Paso | Resultado |
> |---|---|
> | Login → obtener `refresh` token | ✅ |
> | `POST /api/auth/token/refresh/` con refresh token | ✅ 200 + `access` nuevo |
>
> **Conclusión:** El ciclo de vida del token funciona correctamente sin necesidad de volver a hacer login.

---

### 4.4 — Estado de autenticación en frontend

> [!tip] PR-ACC-04 · AuthContext — estado inicial
>
> **Archivo:** `frontend/tests/unit/AuthContext.test.tsx`
>
> | Escenario | Resultado esperado | Resultado |
> |---|---|---|
> | Sin datos en localStorage | `isAuthenticated = false` · email `none` | ✅ |
> | Token y user en localStorage | `isAuthenticated = true` · email correcto | ✅ |
>
> **Conclusión:** La app arranca correctamente con o sin sesión previa guardada.

---

### 4.5 — Login y logout en frontend

> [!tip] PR-ACC-05 · Flujo de sesión en cliente
>
> **Archivo:** `frontend/tests/unit/AuthContext.test.tsx`
>
> | Acción | Resultado esperado | Resultado |
> |---|---|---|
> | `login(token, user)` | Guarda en localStorage + `isAuthenticated = true` | ✅ |
> | `logout()` | Borra localStorage + `isAuthenticated = false` | ✅ |

---

### 4.6 — UI según estado de sesión (Header)

> [!tip] PR-ACC-06 · Header adapta la interfaz al estado de autenticación
>
> **Archivo:** `frontend/tests/flow/Header.flow.test.tsx`
>
> | Estado | UI esperada | Resultado |
> |---|---|---|
> | No autenticado | Botón "Sign In" visible, sin perfil | ✅ |
> | No autenticado → click "Sign In" | Modal de login se abre | ✅ |
> | Autenticado | Link "My Bookings" + menú de perfil | ✅ |

---

### 4.7 — Mis reservas: acceso anónimo bloqueado en frontend

> [!tip] PR-ACC-07 · MyBookings requiere sesión activa
>
> **Archivo:** `frontend/tests/flow/MyBookings.flow.test.tsx`
>
> | Estado | Comportamiento | Resultado |
> |---|---|---|
> | Sin sesión | Muestra "Sign in to see your bookings." · NO llama API | ✅ |
> | Con sesión | Lista reservas del usuario · llama `myReservations()` | ✅ |

---

### 4.8 — Creación de reserva requiere autenticación (backend)

> [!warning] PR-ACC-08 · Reservas protegidas en API
>
> **Archivo:** `backend/tests/test_reservations_api.py`
>
> | Intento | Resultado |
> |---|---|
> | `POST /api/reservations/` sin token | 401 Unauthorized ✅ |
> | `GET /api/reservations/my/` sin token | 401 Unauthorized ✅ |
> | Con token válido | 201 / 200 ✅ |

---

### 4.9 — Serializer de registro crea usuario y perfil

> [!info] PR-ACC-09 · Creación de cuenta a nivel de modelo
>
> **Archivo:** `backend/tests/test_serializers_unit.py`
>
> | Check | Resultado |
> |---|---|
> | `User.username` == email | ✅ |
> | `UserProfile.phone` guardado | ✅ |
> | Email duplicado rechazado | ✅ |

---

### 4.10 — Cancelación de reserva solo por propietario

> [!danger] PR-ACC-10 · Control de propiedad en cancelación
>
> **Archivo:** `backend/tests/test_reservations_api.py`
>
> | Escenario | Resultado |
> |---|---|
> | Usuario cancela su propia reserva | 200 OK · status → `cancelled` ✅ |
> | Usuario cancela reserva ajena | 404 Not Found ✅ |
> | ID inexistente | 404 Not Found ✅ |

---

### 4.11 — Mis reservas devuelve solo las del usuario activo

> [!warning] PR-ACC-11 · Aislamiento de datos por usuario
>
> **Archivo:** `backend/tests/test_reservations_api.py`
> **Test:** `test_my_reservations_only_returns_current_user`
>
> Usuario A tiene 1 reserva · Usuario B tiene 1 reserva
>
> | Consulta | Resultado |
> |---|---|
> | Usuario A → `GET /api/reservations/my/` | Solo 1 reserva (la suya) ✅ |

---

### 4.12 — Búsqueda de restaurantes (acceso sin login)

> [!success] PR-ACC-12 · Listado público con filtros
>
> **Archivo:** `backend/tests/test_restaurants_api.py`
>
> | Filtro | Resultado |
> |---|---|
> | Sin filtros | Todos los restaurantes ordenados por rating ✅ |
> | `?search=sushi` | Filtra por nombre ✅ |
> | `?search=italian` | Filtra por cocina ✅ |
> | `?cuisine=Japanese` (case insensitive) | Filtra correctamente ✅ |

---

### 4.13 — Wrappers de API: auth y reservations

> [!tip] PR-ACC-13 · Clientes frontend llaman endpoints correctos
>
> **Archivo:** `frontend/tests/api/services.api.test.ts`
>
> | Llamada | Endpoint verificado | Resultado |
> |---|---|---|
> | `authApi.register(payload)` | `POST /auth/register/` | ✅ |
> | `authApi.login(payload)` | `POST /auth/login/` | ✅ |
> | `reservationsApi.create(payload)` | `POST /reservations/` | ✅ |
> | `reservationsApi.myReservations()` | `GET /reservations/my/` | ✅ |
> | `reservationsApi.cancel(id)` | `DELETE /reservations/77/` | ✅ |

---

### 4.14 — Smoke: flujo completo de acceso

> [!success] PR-ACC-14 · Flujo end-to-end de acceso completo
>
> **Archivo:** `backend/tests/test_smoke_system.py`
>
> ```
> 1. POST /api/auth/register/     → 201 ✅
> 2. POST /api/auth/login/        → 200 + JWT ✅
> 3. POST /api/reservations/      → 201 ✅  (con JWT)
> 4. GET  /api/reservations/my/   → 200 + 1 reserva ✅  (con JWT)
> ```

---

---

## 💾 5. Copias de Seguridad

> [!quote] ¿Qué se valida?
> Persistencia de datos entre reinicios, estrategia de backup de la base de datos, y verificación de que los datos no se pierden en operaciones normales de despliegue.

---

### 5.1 — Persistencia de SQLite con volumen Docker

> [!success] PR-BAK-01 · Base de datos sobrevive al reinicio del contenedor
>
> **Archivo:** `docker-compose.yml`
> **Verificación:** manual — `docker compose down` + `docker compose up` con volumen `db_data`
>
> | Operación | Datos persistidos |
> |---|---|
> | `docker compose down` + `up` | ✅ Todos los datos intactos |
> | `docker compose down -v` (destruye volumen) | ⚠️ Datos eliminados — uso intencional |
>
> **Configuración:** Volumen `db_data` montado en `/app/data/db.sqlite3`
> **Conclusión:** Los datos persisten entre ciclos normales de contenedor.

---

### 5.2 — Migraciones automáticas al arrancar

> [!success] PR-BAK-02 · Esquema de BD actualizado en cada deploy
>
> **Archivo:** `Dockerfile` — entrypoint
> **Verificación:** revisión de logs de arranque en Railway
>
> ```
> Running migrations...  ✅
> Seeding database...    ✅
> Starting Gunicorn...   ✅
> ```
>
> | Check | Estado |
> |---|---|
> | Migraciones aplicadas automáticamente | ✅ |
> | Seed no destruye datos existentes (idempotente) | ✅ |
>
> **Conclusión:** El esquema siempre está en el estado correcto tras un deploy.

---

### 5.3 — Base de datos en Railway (PostgreSQL)

> [!success] PR-BAK-03 · PostgreSQL gestionado en producción
>
> **Plataforma:** Railway.app — Plugin PostgreSQL
> **Verificación:** panel de Railway → Service database → estado
>
> | Aspecto | Estado |
> |---|---|
> | Servicio PostgreSQL activo | ✅ |
> | `DATABASE_URL` inyectada automáticamente | ✅ |
> | Backups automáticos gestionados por Railway | ✅ |
> | Conexión cifrada SSL a la BD | ✅ |
>
> **Conclusión:** Los datos en producción están en un servicio gestionado con backups automáticos de la plataforma.

---

### 5.4 — Secrets y configuración no se pierden en redeploy

> [!success] PR-BAK-04 · Variables de entorno persistentes entre deploys
>
> **Plataforma:** Railway — panel Variables
> **Verificación:** manual — redeploy desde Railway sin reconfigurar variables
>
> | Variable | Persiste entre deploys |
> |---|---|
> | `SECRET_KEY` | ✅ |
> | `ANTHROPIC_API_KEY` | ✅ |
> | `DATABASE_URL` | ✅ (autogenerada por Railway) |
> | `ALLOWED_HOSTS` | ✅ |
>
> **Conclusión:** La configuración de producción es permanente y no requiere reconfiguración manual tras cada deploy.

---

---

## 🔗 Links relacionados

- [[Mapa Visual de Tests]] — Suite completa de tests automatizados
- [[Security Incidents]] — Vulnerabilidades detectadas y estado
- [[Railway Deployment]] — Entorno de producción
- [[Docker Setup]] — Persistencia en desarrollo local
- [[Frontend Testing - Vitest + Testing Library]] — Setup del framework de tests frontend
- [[Resultados de tests - 2026-04-20]] — Última ejecución completa
